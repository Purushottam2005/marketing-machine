#
# These settings are used when the project is launched from the development environment
#

http.server.port = 12121

# poll every 10 seconds
poll.cron.expression=/10 * * * * ?

idempotent.store.dir = ${java.io.tmpdir}/mule-marketing-machine
